machine:
  ruby:
    version: ruby-2.1.9

test:
  override:
    - exit 0

deployment:
  aws:
    branch: master
    commands:
      - bundle exec bower install
      - bundle exec jekyll build
      - aws s3 sync _site/ s3://type2fun.net/ --region eu-west-1
